# Отчет об изменениях перед коммитом

## Измененные файлы и логика
- **`pages/Home.tsx`**: 
  - Переработана интерактивность кнопок в главном блоке (Hero): удален резкий сдвиг вверх при наведении мыши, добавлена плавная смена цвета и элегантное зеленоватое свечение (`shadow-[0_8px_30px_rgb(34,197,94,0.3)]`), время анимации установлено на 0.3s.
  - Удалена избыточная анимация увеличения (`group-hover:scale-110`) для иконок внутри карточек услуг ("Наши услуги"). Теперь при наведении на карточку срабатывает только её общая анимация подъема, без излишней мультипликации иконки.

## Регрессии
Не обнаружено. Изменения касаются только визуальных эффектов (Tailwind классов) при взаимодействии с элементами. 

## Безопасность
Утечек конфиденциальных данных нет. Логика не затронута.
