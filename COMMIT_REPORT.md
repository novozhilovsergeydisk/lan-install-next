# Отчет об изменениях перед коммитом

## Измененные файлы и логика
- **`.gitignore`**: Добавлен файл `ASSIGNMENTS.md` в список исключений.
- **Git Index**: Файл `ASSIGNMENTS.md` удален из отслеживания репозитория (команда `git rm --cached`). Теперь журнал выполненных задач хранится только локально и не попадает в публичный репозиторий на GitHub.

## Регрессии
Не обнаружено. Изменения касаются только конфигурации Git.

## Безопасность
Улучшена приватность: журнал с финансовой информацией и статистикой работы (ASSIGNMENTS.md) больше не передается в облако.
