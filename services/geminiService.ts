
// Этот сервис больше не используется, так как ИИ отключен.
// Файл оставлен пустым во избежание ошибок импорта, если они где-то остались.
export const sendMessageToGemini = async () => {
  return "Сервис отключен.";
};
